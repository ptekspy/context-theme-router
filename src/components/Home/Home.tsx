import './Home.css';

export default function Home() {
  return (
    <>
      <main className="page-main">
        <h2>Home: Functional [web3] Design in React on IPFS</h2>
        <p>switch pages to "about" to use react router</p>
        <p>refresh page and state remains (TODO: add a state reset to clear local storage)</p>
        <p>Perhaps I will CICD this to an IPFS set up tbd... for now it's on IPFS</p>
      </main>
    </>
  );
}
